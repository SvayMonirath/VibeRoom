/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html",  // all HTML files in src folder
    "./src/**/*.js",    // optional: JS files if you use class names in JS
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
